Paste this exactly, with nothing added before or after it: name: Send Relationship Wisdom Message on: schedule: - cron: '30 10 * * *' workflow_dispatch: jobs: send-message: runs-on: ubuntu-latest steps: - name: Check out repository uses: actions/checkout@v4 - name: Set up Python uses: actions/setup-python@v5 with: python-version: '3.11' - name: Install dependencies run: pip install requests - name: Send today's relationship message env: TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }} TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }} run: python send_relationship.py
